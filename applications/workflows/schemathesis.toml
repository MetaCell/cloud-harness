[phases.coverage]
unexpected-methods = ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"]

# Disable negative testing - don't test with unknown/invalid properties
[phases.negative]
enabled = false

# Set previous_search_token to empty to avoid invalid token generation
# Since this is an optional parameter used for pagination, omitting it is valid
[parameters]
previous_search_token = null