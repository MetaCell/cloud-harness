harness:
  name:
  subdomain:
  domain:
  secured: false
  deployment:
    auto: true
    replicas: 1
    image:
    name: proxy-deployment
    port: 8081
    resources:
      requests:
        memory: "32Mi"
        cpu: "25m"
      limits:
        memory: "500Mi"
        cpu: "500m"
  service:
    auto: true
    name:
    port: 80