pytest
pytest-cov
pytest-mock
coverage
nose
randomize
pyparsing
python-keycloak
# cloudharness.utils.server imports connexion. It is not a runtime dependency of the
# package, as only the flask applications use that module and they bring their own, but
# the test environment needs it to import the module under test. Kept aligned with
# infrastructure/common-images/cloudharness-flask/requirements.txt
connexion[swagger-ui,flask,uvicorn]>=3.0.0,<4.0.0
-e ../models
-e .
